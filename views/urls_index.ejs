<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
    <title>TinyApp</title>
    <style>
    body{
/*      background-image: url("http://www.pptbackgrounds.org/uploads/lined-paper-powerpoint-backgrounds.jpg");
      background-repeat: repeat-x;*/
    }
    </style>
  </head>
  <body style="font-size:20px; padding:50px;">
    <% include ./partials/_header %>
     <% if (!user){ %>
      <h3 style="margin-top: 1em;">ready to C-H-O-P it down?</h3>
      <hr align="left" width="300" style="border: 1px dashed #0066ff">
      <hr align="left" width="250" style="border: 1px dashed #0066ff">
      <hr align="left" width="200" style="border: 1px dashed #0066ff">
      <hr align="left" width="150" style="border: 1px dashed #0066ff">
      <hr align="left" width="100" style="border: 1px dashed #0066ff">
      <hr align="left" width="50" style="border: 1px dashed #0066ff">
     <%} else { %>
      <h2>My URLs</h2>
      <ul>
        <% for (let shortURL in urls) {%>
        <li><b><%= shortURL %>:</b> <%= urls[shortURL] %></li>
        <div class="form-inline">
        <form method="POST" action="/urls/<%=shortURL %>?_method=DELETE"><button type="submit" class="btn btn-danger" style="margin-right: 0.5em">Delete</button></form>
        <a href='/urls/<%=shortURL %>'><button type="submit" class="btn btn-primary">Edit</button></a>
        </div>
        <%}; %>
      </ul>
    <%}%>
  </body>
</html>