<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
    <title>TinyApp</title>
  </head>

  <body style="font-size:20px; padding:50px;">
    <% include ./partials/_header %>
    <% if (!user){ %>
      <h4>Please log in fist</h4>
    <%} else {%>
      <% if (currentUser == userOfURL){ %>
        <h5>Tiny URL for <%= longURL %></h5>
        <h4 style="color: #0066ff"><%= shortURL %></h4>
        <div style="border-style: dashed; border-width: 0.5px; width: 300px; border-color: #0066ff; padding: 10px">
        <p>Total views: <%= views %></p>
        <p>Created on : <%= createDate %></p>
        </div>
        <form class="form-inline" action="/urls/<%=shortURL %>?_method=PUT" method="POST">
          <div class="form-group mb-2">
            <label for="longURL">Update the URL:</label>
            <input class="form-control" type="text" name="longURL" placeholder="http://" style="width: 300px; margin: 1em">
            <button type="submit" class="btn btn-primary">Update</button>
          </div>
        </form>
      <%} else {%>
        <h4>this doesnt belong to you</h4>
      <%}%>
    <%}%>
  </body>
</html>